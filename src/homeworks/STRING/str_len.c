//To find the length of the string.

#include<stdio.h>
#include<string.h>

int main()
{
    char str[] = "Kaali Billi";
    printf("%lu\n", strlen(str));


    // int length;
    // length = sizeof(str) / sizeof(str[0]);
    // printf("%d", length - 1);
}